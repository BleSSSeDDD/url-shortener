version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - revive
    - ineffassign
    - misspell
    - gosec
    - unconvert
    - gocritic

formatters:
  enable:
    - gofmt